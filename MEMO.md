# Kami Hair Solver 開発メモ

このリポジトリは、要件に合わなかった試作を破棄した記録として残す。
作り直す前に、[`ソルバー仕様書.md`](ソルバー仕様書.md)を全文読み、設計レビューを行うこと。

## 最重要事項

**この髪ソルバーを XPBD / PBD の位置拘束投影方式で作ってはいけない。**

LLMへ実装を依頼するとXPBDを選びやすいが、本案件ではXPBDは不採用である。
「実装しやすい」「安定して見える」「リアルタイム向き」という理由で、有限要素・
接触系の要求をXPBDへ置き換えてはならない。

## 破棄した理由

- 旧実装は伸び・曲げ・接触をXPBDの位置投影で処理していた。
- 接触の位置補正が速度へ変換され、反発ゼロでも数値的な運動エネルギーを生成した。
- 弾性と接触を同じエネルギー／残差系で解く有限要素接触ソルバーではなかった。
- したがって、要求された髪の物理シミュレーション方式を満たしていない。

## 将来作り直す場合の必須方針

- Blender Hair Curvesを入力として扱う。
- ロッド／ビームの有限要素エネルギーに基づく定式化を使う。
- 接触を有限要素の時間発展と連成し、同じ残差・ヤコビアン系で解く。
- PPF Contact SolverやIPC系のような、Newton反復を用いる接触方式を検討する。
- 貫通、エネルギー収支、静止安定性を回帰試験で測定する。
- 数式、未知量、エネルギー、接触ポテンシャル、時間積分法を先に確定し、
  それらが承認されるまでコードを書かない。

XPBD版のコードを再利用・復活させないこと。
