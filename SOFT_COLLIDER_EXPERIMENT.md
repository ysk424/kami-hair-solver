# softコライダー実験記録

## 結論

PPFの「静的オブジェクトを完全固定せず、目標形状へ戻すエネルギーを持つ未知数として接触と同時に解く」という考え方は、KAMIにも試す価値があります。ただし、KAMIのBODYを汎用soft bodyへ置き換えるのではなく、従来の処方運動が髪を横切る時間区間だけ有効になる準静的な退避自由度として実装します。

この方式なら、解が存在する普通の区間はkami-solver-2と同じ経路を通り、接近によって処方運動の実行可能解が失われる直前だけBODY側を局所的に譲らせられます。実験版は `kami-solver-3` / version 0.7.2 / ABI 8 として分離し、stable版のExtensionと同時に有効化できます。

## 数理モデル

入力コライダーの各三角形の初期面積を三頂点へ1/3ずつ集中させ、頂点 `i` の集中面積を `A_i` とします。実位置 `x_i` とその時刻のアニメーション目標 `x_i*` に対し、次のアンカーエネルギーを接触バリアへ加えます。

`E_anchor = 1/2 k_a sum_i A_i ||x_i - x_i*||²`

`k_a` はUIの「コライダーアンカー剛性」で単位は N/m³ です。従って頂点の等価ばねは `k_i = k_a A_i` [N/m] になり、メッシュ密度を変えても面積当たりの剛性を概ね維持できます。既定候補は `1e8 N/m³` です。値を大きくすると元アニメーションへ近づき、小さくすると接触時の退避量が増えます。

接触バリアの髪側勾配と反対符号の反力を、接触点の三角形重心座標でBODY三頂点へ配分します。髪は従来のストランド6x6ブロック三重対角、BODYは独立3x3ブロックで方向を求め、両者を同じCCDとArmijo line searchで更新します。KAMIの既存ストランドソルバーを壊さないため、髪—BODYの線形off-diagonalとBODY頂点間のoff-diagonalは意図的に省略しています。

## ハイブリッド切替

softを有効にしても、各時間区間の最初に従来の処方コライダー移動を試します。

1. 目標まで安全に移動できれば、BODY実位置を目標へ一致させ、従来と同じhard経路で髪だけを解きます。
2. 相対運動sweepがTOI制限を要求した場合は、BODYを区間開始位置へ戻します。
3. softへ切り替える場合、自由な髪節点は速度予測位置ではなく区間開始時の実行可能位置から開始します。慣性項の目標には従来どおり速度・重力予測を使うため、運動を捨てずに初期交差だけを避けます。
4. sweepで切り替わらずhard solveがバリア外、line search失敗、またはNewton上限到達になった場合も、同じ時間区間を開始状態へ巻き戻し、時間幅を半減する前に一度softで再試行します。
5. その区間全体を髪とBODYの連成未知数として解き、BODYの退避と髪の変形の妥協点を探します。収束したBODY実位置を次区間へ持ち越し、次の目標まで安全になれば再びhard経路へ戻ります。

統計は `soft_collider_attempts` が全soft試行数、`soft_collider_retry_attempts` がhard収束失敗後の再試行数、`soft_collider_substeps` が成功したsoft区間数です。失敗したsoft試行もあり得るため、実コストの有無は `soft_collider_attempts` で判断します。

## 実装範囲と意図的な非対応

このモデルはPPFの考え方をKAMIの用途へ縮小したものです。次は実装していません。

- BODYの質量、慣性、運動量保存
- 三角形メッシュの面内伸縮、せん断、曲げ、体積保存
- BODYの自己衝突、髪同士の衝突
- 髪—BODYの完全な連成Hessian
- BODYへ接触インパルスを蓄積する動力学

そのため、これは皮膚や布の材質モデルではありません。頂点は近傍との形状結合なしにアンカーへ戻るので、局所的なへこみを許す「接触の逃げ」です。初期交差を修復する機能でもなく、frame startは従来どおり実行可能でなければなりません。

## 計算コスト

2026-08-22にRTX 5070 Ti、Blender 5.2、実データ `Lumi-1-HOU-4.blend` のframe 1→2で測定しました。設定は8 base substeps、128 maximum substeps、32 Newton iterations、BODY 225,184頂点、髪265,108内部節点です。

| 指標 | hard | soft有効 | 差 |
| --- | ---: | ---: | ---: |
| GPU frame | 266.468 ms | 260.504 ms | -2.2%（測定揺らぎ） |
| step wall time | 280.867 ms | 273.771 ms | -2.5%（測定揺らぎ） |
| prepare wall time | 2.858 s | 3.002 s | +5.0% |
| resident CUDA memory | 525.79 MiB | 568.74 MiB | +42.95 MiB / +8.17% |
| soft fallback intervals | 0 | 0 | — |

このフレームは横切りがなく、softの実位置は目標と一致しました。従って通常時のランタイム増加は測定不能なほど小さく、確実な固定費は約43 MiBのGPUメモリと約5%の準備時間でした。soft自由度は675,552、全自由度は2,185,116です。

fallbackが発動すると、非線形反復とline searchの各trialで、BODY頂点数に比例するアンカー評価、3x3方向計算、座標更新、BVH refitが追加されます。費用は発動区間数、接触候補数、line search回数に強く依存します。問題のない区間をhard fast pathへ戻すことが総費用を抑える主要な仕組みです。

同じ実データで停止箇所を含むframe 11〜13も測定しました。frame 11は従来12.071秒から31.638秒（2.62倍）、frame 12は17.892秒から66.589秒（3.72倍）でした。frame 13は従来版が約493秒後に停止したのに対し、0.7.1はNewton上限64で164.10秒で完了しました。成功時はsoft成功20/試行62、BODY最大変形0.077 mm、最小gap 2.25 µmでした。Newton上限32ではsoft solveが上限に達して143.93秒後に停止したため、このデータでは64が必要です。したがって、通常フレームの固定費は小さい一方、接触が厳しくsoft試行が多い近傍フレームには2.6〜3.7倍程度の増加を見込む必要があります。

0.7.2では、髪の予測移動が `max(最大要素自然長, 4 * (半径 + offset + barrier距離))` を超える提案をBVH投入前に縮小します。moving sweepは8,192要素ずつ実行し、一髪要素が512三角形を超えて列挙する提案も時間幅を半減します。softのNewton方向は髪とBODYを同じtrust radius係数で縮小し、line-search CCDはBODY全頂点の最大方向を全検索へpaddingする方式から、実際のBODY方向でrefitしたswept BVHへ変更しました。

同じproductionデータを0.7.2、Newton上限32、8 base substepsでframe 18まで再検証しました。最大可変時間ステップ128ではframe 13が成功40・試行128で約4分後に明示的に終了し、最大予測移動12.212 mm、sweep上限10.000 mm、事前縮小9回、hard Newton上限後soft再試行11回を報告しました。これはGPU停止ではなく設定した試行上限です。同一セッションで最大値だけ256へ変更してframe 13から再開すると、コード変更なしでframe 18まで完了しました。

旧0.7.1がframe 18の表示上の試行45付近で2時間以上GPUカーネルに滞留した条件に対し、0.7.2は同じNewton上限32で試行45を約70秒で通過し、frame 18をGPU 176.28秒、成功45・試行125で完了しました。このフレームでは予測移動最大17.884 mmを10.000 mmへ制限し、事前縮小19回、moving-sweep候補26,126,515、soft成功4/試行65、hard Newton上限後soft再試行11回でした。frame 13から18までの再開wall timeは20分08秒です。softコライダー表示キャッシュを各フレームのアニメーション目標と比較した最大変形はframe 11の0.174 mmで、frame 18では単精度出力の丸め誤差範囲で目標へ戻っています。この結果から、0.7.2の安全弁は無制限滞留を有限の時間ステップ試行へ変換しますが、厳しい区間の総計算量そのものは大きいと判断できます。

表示用BODYキャッシュは1フレーム当たり `12 * collider_vertex_count` bytesです。このデータでは約2.58 MiB/frame、2フレームのヘッダー込み実測は5,404,436 bytesです。version 0.7.2のnative checkpointはモードにかかわらずBODYを倍精度で保持するため、v2に対して1状態当たり `24 * collider_vertex_count` bytes、このデータでは約5.15 MiB増えます。

## 検証結果

- native core testとC API testが成功
- Blender Extension smoke testが成功
- hardでは停止する処方コライダー横切りを、softでは実行可能gapを保って完了
- 速度予測位置が面を越える回帰で、実行可能な区間開始位置からsoft solveを始めることを確認
- hard solveの収束失敗後、時間幅を半減する前に同一区間をsoftで再試行することを確認
- hardのNewton上限到達をsoft再試行へ接続する回帰と、過大予測移動を候補探索前に縮小する回帰を確認
- 上記回帰でsoft fallback、正のアンカーエネルギー、正のBODY変位を確認
- checkpoint save/restore後のBODY出力が倍精度 `1e-14` 許容で一致
- 実データframe 1→2のhard/soft fast pathでgap、反復数、候補数が一致
- 実データframe 13をNewton上限64で完了し、BODY最大変形0.077 mmと実行可能gapを確認
- 0.7.2では実データframe 13の128試行上限を明示エラーとして診断し、上限256への同一フレーム再開だけでframe 18まで完了
- 旧版が2時間以上滞留したframe 18の試行45を約70秒で通過し、GPU 176.28秒・試行125で完了
- 元Extensionとは別IDの「髪3」として両方を同時に有効化

## 採用判断

試験機能として継続評価する価値があります。stable版を変更せず、通常時の実測ランタイムをほぼ維持したまま、従来は停止または長時間滞留した区間を有限の試行として扱えます。一方、soft試行が集中する近傍フレームは2.6〜3.7倍以上遅くなり、0.7.2のframe 13〜18再開にも20分08秒を要しました。BODY形状の物理的妥当性はアンカー密度に依存し、必要なNewton上限と可変時間ステップ上限もシーンによって異なるため、無条件にstable版へ採用する段階ではありません。

本採用の判断条件は次の三点です。

1. 実際に停止するproductionフレームでsoft試行が成功し、計算が先へ進むこと。
2. `collider_maximum_displacement` と出力メッシュを確認し、へこみが許容できること。
3. 発動フレームのGPU時間、line search回数、VRAMが制作上許容できること。

この三点を満たさない場合は、kami-solver-2を維持し、この実験版を採用しないのが妥当です。
