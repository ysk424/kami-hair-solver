# Kami Hair Solver 仕様書

- 文書状態: **設計レビュー前の草案**
- 草案版: 0.1
- 作成日: 2026-08-18
- 対象リポジトリ: `kami-hair-solver`
- ライセンス方針: GPL-3.0-or-later

> [!IMPORTANT]
> この文書が承認されるまで、ソルバー本体の実装を開始してはならない。
> 未確定事項を実装者やLLMが独断で補完してはならない。

## 1. 最重要要件

**このソルバーをPBD、XPBD、または位置拘束投影方式で実装してはならない。**

本プロジェクトが求めるものは、Blender Hair Curvesを入力とする、ロッド／ビーム
有限要素と接触を連成した非線形有限要素接触ソルバーである。

各時間ステップでは、慣性、ロッド弾性、接触、摩擦および境界条件からなる残差と
整合接線行列を組み立て、Newton法と接触を考慮したline searchによって収束解を求める。
接触は弾性と同じ非線形系の中で解かなければならない。

実装上の都合、実行速度、ライブラリ不足、安定性、コード量を理由にXPBDへ変更する
ことを禁止する。指定方式を実装できない場合は、別方式へフォールバックせず作業を停止し、
不足している数式、依存関係または判断を利用者へ質問する。

## 2. 目的

1. BlenderのHair Curvesを、有限要素ロッドとして力学的に計算する。
2. 髪とアニメーションメッシュ、および髪同士の接触を弾性と連成して解く。
3. 接触位置の事後投影ではなく、接触ポテンシャル／接触残差をNewton系に含める。
4. 閉じた系へ非物理的なエネルギーを注入しない。
5. 接触可能な初期状態から開始した場合、許容誤差を超える貫通を発生させない。
6. 約10点で構成された元Hair Curvesを、必要に応じて内部だけ有限要素分割する。
7. Blender上で準備、検査、計算、進捗確認、結果保存を日本語UIから実行できるようにする。

## 3. 対象外

初期版では次を対象外とする。必要になった場合は仕様変更としてレビューする。

- リアルタイム動作の保証
- PBD／XPBDとの互換性
- 位置投影による簡易衝突解消
- 初期貫通した入力を自動的に押し出して正当化する処理
- ガイドヘアからレンダーヘアを補間する機能
- 流体との完全連成
- 毛髪の破断、塑性、永久変形
- 複数方式をUIから切り替える汎用物理フレームワーク

## 4. 用語

- **入力点**: Blender Hair Curvesに元から存在する制御点。
- **内部節点**: 有限要素サイズを満たすため、入力点間へ追加する節点。
- **ストランド**: Hair Curves内の1本のCurve。
- **ロッド要素**: 連続する内部節点間に置く有限要素。
- **接触ギャップ**: 形状間距離から接触厚さを差し引いた値 `g`。
- **接触可能状態**: すべての有効な接触候補について `g > 0` を満たす状態。
- **モノリシック連成**: 慣性、弾性、接触を同じ残差・接線行列へ含めて解くこと。

## 5. 絶対に禁止する方式

次のいずれかを主要な時間発展または接触解法として使用した時点で、仕様不適合とする。

- Position Based Dynamics（PBD）
- Extended Position Based Dynamics（XPBD）
- Verlet積分と位置拘束投影の組合せ
- 距離拘束、曲げ拘束、接触拘束を逐次位置投影するGauss–Seidel法
- 接触後に頂点を法線方向へ押し戻すだけの処理
- 押し戻した位置差から速度を再生成する処理
- 弾性を解いた後で接触だけを別処理するoperator splitting
- 有限要素エネルギーをXPBDコンプライアンスへ変換する実装
- 固定回数の拘束反復だけで成功と判定する実装
- 「見た目が近い」「高速」「一般的なヘアシム」という理由による方式変更
- 実装が難しい場合のXPBDフォールバック

「エネルギーベース」「有限要素風」「implicit風」という説明だけでは適合と認めない。
残差、接線行列、Newton更新式および収束判定を提示できる必要がある。

## 6. 入力データ

### 6.1 Hair Curves

- 入力はBlender 5.2の `Object.type == "CURVES"` とする。
- ガイドヘアは使用しない。入力された全ストランドを直接シミュレーション対象にする。
- 1本のCurveを1本の開いた毛髪ストランドとして扱う。
- 現在の想定は1ストランド約10点だが、点数を固定値として仮定してはならない。
- ストランドの接続関係は計算中に変化しない。
- 入力オブジェクトを破壊的に変更しない。
- オブジェクト変換、親子関係、アニメーションを評価したワールド座標を入力に用いる。

### 6.2 ゼロ長区間と重複点

- 同一ストランド内の連続点が同位置にある場合、ゼロ長有限要素を作ってはならない。
- 内部メッシュでは重複区間を統合してよいが、元入力点への対応表を維持する。
- 別ストランド同士の点が同位置であることを、同一ストランドの重複点と混同しない。
- 全点が同位置のストランドは入力エラーとして、ストランド番号と点番号を報告する。

### 6.3 衝突ボディ

- Blenderで評価後のMeshを三角形化して使用する。
- 面積ゼロ、非有限座標、重複インデックスを持つ三角形を検出する。
- 除外または修復した面は件数と対象を報告し、検査用コライダープロキシを作成できるようにする。
- アニメーション中に頂点位置は変化してよい。
- 初期版ではトポロジーが時間変化するコライダーを対象外とする。
- 動くコライダーの速度を接触と摩擦の相対速度へ含める。

### 6.4 初期交差

バリア型接触は接触可能な初期状態を必要とするため、準備時に次を検査する。

- 髪とボディの初期交差
- 非隣接髪要素同士の初期交差
- 接触厚さを考慮した負の初期ギャップ
- コライダーの非多様体、反転面、微小面

初期交差を位置投影で押し出して計算続行してはならない。初期版では診断を表示して停止する。
将来自動untangleを導入する場合も、通常の時間発展とは別の明示的な前処理として仕様化する。

## 7. 内部有限要素メッシュ

### 7.1 分割

- すべての元入力点を内部節点として保持する。
- 元入力点間の区間が最大要素長 `h_max` を超える場合、その区間へ内部節点を挿入する。
- 初期案では各元区間を等しい静止長へ分割する。
- 元点番号、ストランド番号、弧長座標、内部節点番号の対応表を保持する。
- 計算中の適応的再メッシュは初期版では行わない。
- 要素長を半分にしたときに解が収束することを試験する。

### 7.2 要素サイズ

`h_max` は単なる速度調整パラメータではなく、次を解像できる値として決定する。

- 初期曲率
- 接触半径
- コライダーの局所曲率
- 曲げ波長
- 出力に必要な形状精度

接触半径だけから自動決定する方式は採用せず、根拠と収束試験を示す。

## 8. ロッド有限要素モデル

### 8.1 採用するモデルの範囲

第一候補は、幾何学的に非線形なCosserat rodまたはKirchhoff rodの有限要素である。
最終選択は実装前レビューで確定する。

最低限、次の物理量を表現する。

- 軸方向伸び
- 曲げ
- ねじり
- 断面半径と断面二次モーメント
- 線密度または体積密度

せん断を自由度として持つCosserat rodにするか、Kirchhoff仮定でせん断を除くかは
未確定事項とする。中心線位置だけを持ち、ねじり自由度を省略したモデルを無断で採用しない。

### 8.2 未知量

候補となる節点未知量を次とする。

```text
q = { x_i, R_i }
```

- `x_i`: 節点位置
- `R_i`: 断面material frameの回転

回転表現にはSO(3)上の増分回転、回転ベクトル、単位Quaternionなどが考えられる。
Newton更新と接線行列が整合する表現を選び、正規化による事後位置投影へ依存しない。

### 8.3 弾性エネルギー

ロッドの静止ひずみを `gamma_0`、静止曲率・ねじりを `kappa_0` とし、概念的には
次のエネルギーを要素積分する。

```text
E_rod(q) = 1/2 ∫ (gamma - gamma_0)^T K_se (gamma - gamma_0) ds
         + 1/2 ∫ (kappa - kappa_0)^T K_bt (kappa - kappa_0) ds
```

- `K_se`: 伸び・せん断剛性
- `K_bt`: 曲げ・ねじり剛性
- 円形断面では `A = pi r^2`、`I = pi r^4 / 4`、`J = pi r^4 / 2` を基準にする。
- Young率、Poisson比またはせん断弾性率から構成則を定義する。
- 初期カールと初期ねじりを静止状態として保持できるようにする。

実装前に要素エネルギー、一次微分、二次微分を設計書へ追記する。
数値差分または自動微分との比較で勾配とHessianを検証する。

### 8.4 質量と慣性

- 並進質量だけでなく、採用したロッドモデルに必要な回転慣性を含める。
- 集中質量行列または整合質量行列の選択を明記する。
- 固定根元は質量をゼロにするのではなく、Dirichlet境界条件として系へ適切に組み込む。
- 動く根元が行う仕事を外部仕事として計測できるようにする。

### 8.5 減衰

- 位置補正に由来する暗黙の減衰を材料減衰と呼んではならない。
- Rayleigh減衰、Kelvin–Voigt型粘性、またはロッド構成則に整合する減衰を候補とする。
- 採用する減衰は残差と接線行列へ整合的に含める。
- 閉じた系のエネルギー試験と、減衰を有効にした単調減衰試験を分ける。

## 9. 時間積分

初期候補はimplicit Eulerとする。Newmark法またはgeneralized-alpha法を採用する場合は、
エネルギー特性と高周波減衰の理由を設計レビューで示す。

位置自由度だけで概念を示すと、予測位置を

```text
x_hat = x_n + dt v_n + dt^2 M^-1 f_ext
```

とし、増分ポテンシャルを概念的に

```text
Phi(x) = 1/2 (x - x_hat)^T M (x - x_hat)
       + dt^2 E_rod(x)
       + dt^2 E_contact(x)
       + dt^2 E_friction(x; x_n)
```

とする。回転自由度を含む最終式は、採用するロッド要素と時間積分に合わせて導出する。

各時間ステップで解く条件は

```text
R(q) = grad Phi(q) = 0
H(q) Delta q = -R(q)
q_next = q + alpha Delta q
```

である。`H`は整合接線行列または目的関数のHessianとする。

## 10. 非線形Newtonソルバー

### 10.1 Newton反復

各反復で次を実行する。

1. 慣性、ロッド弾性、接触、摩擦、境界条件を評価する。
2. 全項の残差と接線行列を同じ大域系へ組み立てる。
3. 線形系を許容誤差まで解く。
4. CCDと目的関数減少条件を満たすstep lengthを求める。
5. 状態を更新し、残差・増分・接触可能性を再評価する。

### 10.2 収束条件

固定反復回数だけで成功としてはならない。最低限、次を記録して判定する。

- 絶対残差ノルム
- 相対残差ノルム
- Newton増分ノルム
- 目的関数の変化
- 最小接触ギャップ
- line searchの採用率
- 線形ソルバー残差

最大反復回数は失敗上限であり、収束条件ではない。未収束時に結果を書き出して成功扱い
してはならない。

### 10.3 Hessianと線形ソルバー

- 要素ごとの解析的Hessianまたは検証可能な自動微分Hessianを使用する。
- Newton方向を下降方向にするための固有値フィルタ／PSD射影を採用する場合は、
  物理モデルへの影響と適用範囲を明記する。
- 疎行列形式、前処理、PCG／MINRES／疎直接法の選択は設計レビューで決める。
- 単精度を採用する場合も、微分検証と基準試験は倍精度版で実行できるようにする。

## 11. 接触

### 11.1 基本方式

接触はPPF Contact Solver、IPCまたはC-IPCと同系統のバリア型接触を使用する。
最終的なバリア関数は実装前に決定する。

候補は次のいずれかとする。

- PPFのcubic barrierとelasticity-inclusive dynamic stiffness
- IPC／C-IPCのバリアポテンシャル
- 上記と数学的に同等で、勾配・Hessian・CCDが明示された方式

単純な法線ペナルティ、ばね接触、衝突後押し戻しは代替にならない。

### 11.2 接触プリミティブ

- 髪同士: 非隣接edge–edge
- 髪端点とボディ: vertex–triangle
- 髪要素とボディ: edge–triangle、または厚さ付きロッドと三角形の等価な距離
- 必要に応じてvertex–edge

同一ストランドの隣接要素や、同じ節点を共有する要素は自己接触候補から除外する。
何要素先まで除外するかは、要素サイズと曲率を考慮して決める。

### 11.3 厚さ

- 毛髪中心線間の距離に、物理半径または接触用offsetを設定する。
- 髪同士の最小中心線距離は原則として両者の半径の和とする。
- 髪とボディでは髪半径とボディ側offsetを考慮する。
- 表示半径、物理半径、接触活性距離 `d_hat` を混同しない。

### 11.4 CCDとline search

- Newton更新によって新しい交差を作らないよう、各line searchでCCDを実行する。
- edge–edgeおよびvertex–triangleを最低限対象とする。
- 求めたTOIへ安全率を適用し、接触可能領域内だけで更新する。
- CCD失敗、TOIが数値的にゼロ、非有限距離は明示的な計算失敗とする。
- 貫通後に位置を戻す方式ではなく、貫通する更新を最初から採用しない。

### 11.5 摩擦

- Coulomb摩擦を接触相対速度に基づいて扱う。
- 静止摩擦と動摩擦、または正則化摩擦ポテンシャルの選択を明記する。
- 摩擦項もNewton系へ整合的に組み込む。
- 動くボディおよび動く根元の速度を相対速度へ含める。

### 11.6 根元付近の密集

実際のHair Curvesでは、頭皮上の根元間隔が接触直径より小さい可能性がある。
これは通常の初期貫通と区別して設計する必要がある。

候補となる方針は次の通りだが、実装前に1つを選ぶ。

- 根元から指定弧長まで髪同士接触を除外する。
- 根元からの距離に応じて接触半径を滑らかに立ち上げる。
- 毛束または植毛グループ単位の接触maskを入力する。

LLMが独断で全自己接触を無効化してはならない。

## 12. エネルギー要件

### 12.1 区別して記録する量

各フレームで最低限、次を別々に記録できるようにする。

- 並進運動エネルギー
- 回転運動エネルギー
- 伸び・せん断エネルギー
- 曲げ・ねじりエネルギー
- 重力ポテンシャル
- 接触バリアエネルギー
- 摩擦による散逸
- 材料減衰による散逸
- 動く根元が行った仕事
- 動くコライダーが行った仕事
- 数値的なエネルギー収支誤差

### 12.2 必須挙動

- 静止形状、重力なし、外部運動なしでは速度とエネルギーを生成しない。
- 反発ゼロの接触解決が、接触補正だけを原因とする分離速度を生成しない。
- 外力と減衰がない閉じた系では、時間積分法の既知の誤差を超えてエネルギーが増え続けない。
- 減衰あり、外部仕事なしでは、適切に定義した機械エネルギーが長期的に減少する。
- 動く根元またはコライダーによる増加は外部仕事として説明できる。

エネルギー異常を「速度減衰を増やす」だけで隠してはならない。

## 13. 境界条件

- 各ストランドの根元から指定範囲をDirichlet境界条件として固定できる。
- 入力Hair Curvesがアニメーションする場合、固定自由度の目標をフレーム間で連続的に与える。
- 位置だけを固定するか、断面回転も固定するかを指定できるようにする。
- 動く境界条件が行う仕事を測定する。
- 有限要素分割後も「元入力点数」と「内部固定自由度」の意味を混同しない。

## 14. Blender入出力

### 14.1 対象環境

- Blender 5.2 LTS
- 指定実行ファイル:
  `C:\Users\azoo\git\build_windows_Release_x64_vc17_Release\bin\blender.exe`
- Windows x64
- UI、エラー、進捗、主要文書は日本語
- MCPから準備、検査、計算、状態取得を行える構成を考慮する。

### 14.2 準備処理

「シミュレーション用に準備」では、少なくとも次を行う。

1. Hair Curvesとコライダーを評価する。
2. 不正座標、ゼロ長区間、縮退三角形を検査する。
3. 内部有限要素メッシュと元点対応表を作る。
4. 初期交差と最小ギャップを検査する。
5. 要素数、自由度数、接触候補数、推定メモリ量を表示する。
6. 入力を変更せず、必要な検査用オブジェクトだけを別コレクションへ作る。

「準備成功」は、Newtonソルバーを構築でき、初期状態が接触可能であることまで確認して
初めて表示する。

### 14.3 出力点数

内部計算では適切な要素数へ細分化するが、通常出力では元Hair Curvesの点を対応する
内部節点から抽出し、元の関節数へ戻す方針とする。

ただし、内部節点で表現された接触回避形状を約10点へ戻すと、Blender側で点間を結ぶ
表示曲線がコライダーを横切る可能性がある。このため、次の2方式を設計レビューで比較する。

1. **元トポロジー出力**: 元点数を維持し、データ量を抑える。
2. **接触形状保持出力**: 内部節点を出力し、接触形状を維持する。

既定値は元トポロジー出力を候補とするが、貫通表示との矛盾を試験してから確定する。

### 14.4 キャッシュ

- 点ごと・軸ごとに大量のFカーブを作ってはならない。
- 保存量は少なくとも事前に推定して表示する。
- 外部バイナリポイントキャッシュ、Blenderネイティブキャッシュ、Alembic、Shape Key方式などを
  比較し、読み込み速度、ファイルサイズ、可搬性から決定する。
- 計算結果の書き込みにもフレーム進捗、平均時間、残り時間を表示する。
- 計算完了前に部分キャッシュを残す場合は、未完成であることを明示し、安全に削除できるようにする。

### 14.5 進捗表示

最低限、次を表示する。

- 現在フレーム／総フレーム
- 現在Newton反復／上限
- 残差ノルム
- 線形ソルバー反復数
- 接触候補数と有効接触数
- 最小ギャップ
- line search step length／TOI
- 1フレーム平均時間
- 残り時間予測
- CPUスレッド数またはGPU使用状況
- 計算中、線形求解中、CCD中、書き込み中の状態

## 15. 並列化と性能

- 正しさと収束性を確認する前に、解法を簡略化して速度を優先しない。
- 要素エネルギー、接触候補生成、接触評価、行列組立、線形求解を並列化対象とする。
- CPU/OpenMP版を基準実装にするか、最初からCUDA版にするかは未確定とする。
- シングルスレッドでしか動作していない場合、UIにその事実を表示する。
- 性能試験では要素数、ストランド数、接触数、Newton反復数、線形反復数を併記する。
- 「30フレーム何分」という推定は、最初の収束フレームの実測から更新する。

## 16. 数値検証と受入試験

### 16.1 微分検証

- 各要素エネルギーの解析勾配と中心差分を比較する。
- 解析Hessianと勾配の中心差分を比較する。
- 接触バリアと摩擦についても同じ検証を行う。
- ランダム姿勢、ほぼ直線、強曲げ、微小回転を含める。

### 16.2 ロッド単体

- 静止状態保存
- 軸引張りと解析解の比較
- 片持ち梁たわみと参照解の比較
- 曲げ振動の時間刻み収束
- ねじりと曲げ・ねじり連成
- 初期カール形状の保存
- 要素分割を細かくしたときの空間収束

### 16.3 エネルギー

- 重力なし・接触なし・静止状態を長時間計算して速度が発生しない。
- 閉じた自由振動で非物理的なエネルギー増加が継続しない。
- 減衰を有効にした場合、外部仕事なしでエネルギーが減少する。
- 反発ゼロの接触で、位置修正由来の発射速度を生成しない。
- 動く根元から入るエネルギーと計測した外部仕事が整合する。

### 16.4 接触

- 1本の髪と静止平面
- 高速で薄い三角形へ衝突する髪
- 動くコライダーとの接触
- 2本の髪のedge–edge接触
- 多数ストランドの自己接触
- 摩擦なしの滑り
- 静止摩擦による保持
- 長時間静止接触での速度増加なし
- CCDを伴う各Newton更新後に最小ギャップが許容値以上

### 16.5 Blender実機試験

- 実Hair Curvesの読み込み
- 全ストランドが入力され、ガイド補間を使用していないこと
- 約10点から内部細分化されること
- 元点対応表が正しいこと
- コライダーの縮退面診断
- 初期交差の日本語診断
- アニメーション根元とアニメーションコライダー
- キャッシュ保存後の再読み込み
- Blender再起動後も結果が同一であること
- ベイク消去と準備データ削除

## 17. 成功条件

最初のリリースは、少なくとも次を満たす必要がある。

- ソースコード内にPBD／XPBDの更新式が存在しない。
- ロッド有限要素エネルギー、勾配、Hessianが文書化・検証されている。
- 慣性、弾性、接触が同じNewton系へ組み込まれている。
- Newton法が残差基準で収束する。
- CCD line searchにより接触可能性が維持される。
- 反発ゼロ接触が数値的な分離エネルギーを生成しない。
- 静止接触の長時間試験で速度が増加しない。
- 時間刻みと要素サイズを小さくしたときに解が収束する。
- Blender 5.2実機で日本語UIとHair Curves入出力が動作する。
- GPL-3.0-or-laterとして再配布可能な依存関係だけを使用する。

## 18. 実装前に提出する数値設計書

実装担当者またはLLMは、コードを書く前に次を提示する。

1. 採用するロッド理論と自由度
2. 形状関数と数値積分
3. ひずみ、曲率、ねじりの定義
4. 要素エネルギー、勾配、Hessian
5. 質量行列と回転慣性
6. 時間離散式
7. 1時間ステップの目的関数または残差
8. 接触ギャップとバリア関数
9. 接触勾配とHessian
10. 摩擦モデル
11. CCDとline search
12. Newton収束判定
13. 線形ソルバーと前処理
14. エネルギー収支の定義
15. 空間・時間収束試験計画

各数式から実装関数と試験への対応表も作る。この文書のレビュー承認前に、ソルバー本体、
Blenderアドオン、仮のXPBDプロトタイプを作ってはならない。

## 19. 開発順序

1. 本仕様書のレビューと未確定事項の決定
2. 数値設計書の作成
3. 倍精度の単一ロッド有限要素と微分試験
4. 接触なしのimplicit Newton時間発展
5. エネルギー収支試験
6. 静止ボディとのバリア接触とCCD
7. edge–edge自己接触
8. 摩擦と動くコライダー
9. 多数ストランドでの疎行列・接触候補並列化
10. Blender Hair Curves入出力
11. キャッシュ方式の比較と実装
12. Windowsパッケージ化と長時間回帰試験

前段階の受入試験に合格するまで次段階へ進まない。

## 20. 明日の設計レビュー項目

次の項目は利用者の確認後に確定する。

1. Cosserat rod FEMとKirchhoff rod FEMのどちらを採用するか。
2. 断面回転・ねじり自由度を初版から必須とするか。
3. PPF cubic barrierを直接参考にするか、IPC／C-IPC型を採用するか。
4. implicit Euler、Newmark、generalized-alphaのどれを採用するか。
5. CPU/OpenMP基準実装から始めるか、CUDAを前提とするか。
6. 髪の物理半径、接触offset、`d_hat`をどう入力するか。
7. 根元近傍の髪同士接触をどう扱うか。
8. 元約10点へ戻す出力と、接触形状を保つ内部点出力をどう使い分けるか。
9. 許容する最小ギャップ、残差、エネルギー誤差の数値基準。
10. 想定する最大ストランド数、内部節点数、フレーム数、利用可能GPU。
11. Blenderキャッシュを内部形式、外部形式のどちらにするか。
12. PPF Contact Solverの既存rod機能を利用・移植・参照のどこまで許可するか。

## 21. LLMへ渡す必須指示

新しいセッションでは、依頼の冒頭に次をそのまま渡す。

```text
最初にソルバー仕様書.mdを全文読んでください。
このプロジェクトではPBD、XPBD、位置拘束投影を全面的に禁止します。
有限要素エネルギーをXPBD拘束へ変換することも禁止します。

実装前に、未知量、ロッド有限要素エネルギー、時間離散、接触バリア、
残差、Hessian、Newton法、CCD line search、収束条件を数式で提示してください。
慣性・弾性・接触は同じ非線形系へ組み込んでください。

指定方式を実装できない場合、簡易方式へ変更せず停止して質問してください。
数値設計書が承認されるまでコードを書かないでください。
```

LLMが「まず動くXPBD版を作る」「後でFEMへ置き換える」と提案した場合、その提案を却下する。
XPBDプロトタイプは将来のFEM実装の土台にはならず、検証対象も異なるため作成しない。

## 22. 参照資料

- ローカル参照: `../ppf-contact-solver`
- ZOZO's Contact Solver: <https://github.com/st-tech/ppf-contact-solver>
- A Cubic Barrier with Elasticity-Inclusive Dynamic Stiffness:
  <https://doi.org/10.1145/3687908>
- Incremental Potential Contact: <https://ipc-sim.github.io/>
- Codimensional Incremental Potential Contact: <https://ipc-sim.github.io/C-IPC/>
- Discrete Elastic Rods: <https://doi.org/10.1145/1360612.1360662>
- Position Based Dynamics（禁止方式の識別用）:
  <https://diglib.eg.org/items/deb0a7a1-2ddf-496f-889a-fe0df1feeb73>
- XPBD（禁止方式の識別用）: <https://doi.org/10.1145/2994258.2994272>

## 23. 文書の優先順位

本仕様書と実装、コメント、LLMの提案が矛盾する場合、本仕様書を優先する。
ただし、「明日の設計レビュー項目」と明記した箇所は未確定であり、利用者の判断を優先する。

仕様変更は口頭の推測で行わず、このMarkdownを先に更新してから実装へ反映する。
